void getNumberPattern(int n) {
    int size = 2*n-1;
    for(int i=0; i<size; i++) {
        for(int j=0; j<size; j++) {
            int minDis = min(min(i, j), min(size-i-1, size-j-1));
            cout << (n-minDis);
        }
        cout << endl;
    }
}
