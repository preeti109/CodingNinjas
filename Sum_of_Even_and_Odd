#include<iostream>
using namespace std;

int main() {
	int n;
	cin >> n;
	int even, odd = 0;
	while(n!=0) {
		int digit = n%10;
		if(digit%2==0) even += digit;
		else odd += digit;
		n /= 10;
	}
	cout << even << " " << odd;
	return 0;	
}
