#include <bits/stdc++.h> 
#include <vector>

vector < int > termsOfAP(int x) {   
    int i=1, count=0;
    vector<int> v;
    while(count != x){
        if((3*i+2)%4 != 0){
            v.push_back((3*i+2));
            count++;
        }       
        i++;
    }
    return v;
}
